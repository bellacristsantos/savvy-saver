function CompanyList({ companylist }) {
  return (
    <div
      style={{ display: 'flex', flexDirection: 'row', justifyContent: 'space-between' }}
    >
      <div>companylist.symbol</div>
      <div>companylist.name</div>
    </div>
  )
}

export default CompanyList
